# Overview

`citylens-core` is a lightweight, installable Python package that runs the Citylens pipeline locally.

- Inputs: a `CitylensRequest` and a local `work_dir`.
- Outputs: standard artifacts written into `work_dir` plus a returned mapping of artifact paths.
